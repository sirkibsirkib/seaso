main {
	task agent data as=assignment(a,t)
	assigned(t) !$unassigned(t) !$overworked(a) 
	$eq=eq_agent(a,a) [eq(A,A)] +A
	[assigned(T)] +as(A,T) [unassigned(T)] +T +as(T)
	[overworked(A)] +as(A,T1) +as(A,T2) -eq(T1,T2)
}

plan {
	$ag(str)=main.agent $d(int)=main.data
	$as=main.assignment $t(int)=main.task
	[as(ag("Amy"),t(2))]
	[as(ag("Bob"),t(3))]
}
