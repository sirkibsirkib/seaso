defn known(). unknown1(). unknown2().

# these rules alone result in two unknowns (as per the names)
rule unknown1() :- !unknown2(), known().
     unknown2() :- !unknown1().
     known().

# ... but this fixes it. "unknown2" is true, hence "unknown1" is false.
rule unknown2() :- knowan().
